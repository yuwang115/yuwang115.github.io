Original prompt: 我现在想将我的3D Antarctica Explorer升级为3D Antarctica&Greenland Explorer，添加选项，用户可以自由切换显示Antarctica或Greenland，我在下面的文件夹内为你准备好了所需的BedMachineGreenland数据，先实现基础的Greenland Ice和bed显示功能：

/Users/eddie/Library/CloudStorage/OneDrive-UniversityofTasmania/Documents/Greenland_Dateset/BedMachineGreenland

- 2026-03-07: Inspected current explorer structure. Main entry is `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`, with terrain packaged as `meta.json + bin`.
- 2026-03-07: Confirmed Greenland source folder contains `BedMachineGreenland-v6.nc` and metadata sidecars, but browser cannot consume them directly.
- 2026-03-07: Plan is to reuse the Antarctica terrain packaging format for Greenland, then add a region selector in the explorer and disable unsupported Greenland overlays for now.
- 2026-03-07: Added `scripts/prepare_bedmachine_greenland.py` and generated `static/tools/data/bedmachine_greenland_v6_3km.meta.json` + `.bin` plus `static/tools/data/bedmachine_greenland_v6_1km.meta.json` + `.bin` from `BedMachineGreenland-v6.nc`.
- 2026-03-07: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to support Antarctica/Greenland region switching, dynamic resolution options, region-aware metadata, and Greenland-only capability gating for unsupported overlays.
- 2026-03-07: Added minimal `window.render_game_to_text` and `window.advanceTime` hooks for browser automation checks.
- 2026-03-07: Verified with Playwright that `?region=greenland` renders correctly and that UI-based switching Antarctica -> Greenland -> Antarctica works without console errors. Screenshots saved under `/Users/eddie/Documents/yuwang115.github.io/output/ui-region-switch/`.
- 2026-03-07: Replaced Greenland's single 3.6 km preset with two presets: 3 km and 1 km. Verified Greenland 3 km -> 1 km -> 3 km switching in-browser.
- 2026-03-07: Removed the extra right-aligned Region/Resolution label values from the control panel so only the dropdowns show the current selection.
- 2026-03-07: Added `scripts/prepare_greenland_velocity.py` to resample ITS_LIVE Greenland velocity onto the BedMachine Greenland 3 km and 1 km grids, then generated `static/tools/data/greenland_ice_velocity_3km.meta.json` + `.bin` and `static/tools/data/greenland_ice_velocity_1km.meta.json` + `.bin`.
- 2026-03-07: Optimized the Greenland velocity packager to read source rows in chunks so the 1 km export finishes in practical time instead of per-row netCDF reads.
- 2026-03-07: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to enable Greenland `Show ice surface velocity` and `Show ice flowline`, wire both Greenland presets to the new ITS_LIVE velocity packages, and disable background warmup / decimate the velocity overlay mesh for the 1 km preset.
- 2026-03-07: Verified with `WEB_GAME_CLIENT` and Playwright that Greenland 3 km loads cleanly, velocity and flowline overlays render without console errors, and switching 3 km -> 1 km -> 3 km preserves and rebuilds both overlays correctly. Screenshots/state saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-velocity-smoke-final/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-velocity-validation/`.
- 2026-03-08: Added `scripts/prepare_greenland_basins.py` to convert `Greenland_Basins_PS_v1.4.2.shp` into explorer JSON and generated `static/tools/data/greenland_basins_ps_v1_4_2.json` with 7 Greenland basin sectors in EPSG:3413.
- 2026-03-08: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so basin overlays are region-aware instead of hard-wired to Antarctica: each region now supplies its own basin dataset URL and toggle label, basin data caches by URL, and `render_game_to_text` reports basin toggle/mesh state.
- 2026-03-08: Enabled Greenland basin overlays (`Show basins`) on both 3 km and 1 km presets while preserving Antarctica's existing `Show refined basins` behavior. Verified Greenland 3 km basins, Greenland 1 km basins, Antarctica balanced basins, and Greenland <- -> Antarctica switching without console errors. Screenshots/state saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-basins-smoke-2/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-basins-validation/`.
- 2026-03-08: Reformatted all `Resolution preset` dropdown items to the `Label (x km grid; ~x MB)` style, including Antarctica `Balanced/HD` and Greenland `3 km/1 km`.
- 2026-03-08: Expanded `Data Snapshot` to show region, preset, terrain package size, grid resolution, projection, velocity coverage/stats, basin status, hydrology status, and concise data-product identifiers. Basin metadata now prefetches into the snapshot even before the overlay is toggled on.
- 2026-03-08: Verified the updated preset text and `Data Snapshot` content in-browser for both Greenland and Antarctica. DOM text and screenshots saved under `/Users/eddie/Documents/yuwang115.github.io/output/data-snapshot-validation/`.
- 2026-03-08: Renamed Greenland `Resolution preset` entries to `Balanced` and `HD`, and increased Greenland basin label size without changing Antarctica's basin label scale. Verified in-browser under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-balanced-validation/`.
- 2026-03-08: Set Greenland's default camera pose to `position (3.36, 81.62, 82.79)`, `target (5.14, -11.77, 6.48)`, `fov 45`, and tightened `Reset Camera` so OrbitControls damping does not leave the camera slightly off-pose after a drag. Verified under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-camera-validation/`.
- 2026-03-08: Reworked `Data Snapshot` back toward the earlier Antarctica-style scientific summary: removed internal file/package identifiers, restored a concise `Sources` section, kept projection/coverage/basin-count metadata, and verified Antarctica + Greenland snapshot text/screenshots under `/Users/eddie/Documents/yuwang115.github.io/output/data-snapshot-style-validation/`.
- 2026-03-08: Unified the public-facing tool title across the HTML document title, homepage showcase card, tools-page card, and iframe accessibility labels to `3D Antarctica & Greenland Explorer 🇦🇶`. Skipped testing per user request.
- 2026-03-08: Disabled fullscreen in homepage `showcase` mode at both the logic and UI levels, and restructured the homepage showcase CTA so the `Tools page` link sits outside the iframe wrapper with a higher stacking context and a larger tap target. Verified with Playwright across desktop/tablet/mobile and with `WEB_GAME_CLIENT` showcase smoke under `/Users/eddie/Documents/yuwang115.github.io/output/homepage-showcase-validation/` and `/Users/eddie/Documents/yuwang115.github.io/output/showcase-fullscreen-smoke/`.
- 2026-03-08: Refined the homepage showcase fix after identifying the real cause: OrbitControls pointer capture could keep showcase mode active after dragging out of the iframe. The homepage CTA was restored to the simpler inline text style, and showcase interaction now auto-deactivates on pointer release/cancel/lost-capture outside the viewer. Re-verified fullscreen hiding plus drag-out -> click `Tools page` navigation with real mouse-coordinate clicks on desktop and tablet, and kept the existing showcase smoke artifacts.
- 2026-03-08: Added `scripts/prepare_greenland_ocean_currents.py` to convert the Copernicus monthly Arctic physics NetCDF into a sparse Greenland ocean-current package in EPSG:3413, sampling six depth levels (0, 50, 150, 300, 600, 1000 m), clipping against the BedMachine Greenland ocean mask, and writing `static/tools/data/greenland_ocean_currents_cmems_202508.meta.json` + `.bin`.
- 2026-03-08: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to add a Greenland-only `Show 3D ocean currents` toggle, load the sparse Copernicus package on demand, and render colored 3D line-arrow vectors around Greenland. Arrow direction uses monthly mean `u/v/w`, while color blends temperature and salinity into a water-mass palette.
- 2026-03-08: Expanded Greenland `Data Snapshot` and `Sources` to include the new ocean-circulation layer, sparse vector count, monthly reference time (`August 2025`), depth levels, horizontal-speed range, and temperature/salinity span without exposing internal file names.
- 2026-03-08: Verified Greenland ocean currents with `WEB_GAME_CLIENT` and Playwright: default Greenland load, toggle-on of 3D ocean currents, and `Balanced -> HD -> Balanced` preset switching all succeeded with no console errors. Artifacts saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-currents-smoke/`, `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-currents-smoke-2/`, and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-currents-validation/`.
- 2026-03-08: Reworked the Greenland ocean-circulation package after user feedback to render continuous 3D ocean flowlines instead of colored arrows. `scripts/prepare_greenland_ocean_currents.py` now traces layered streamlines from the sparse Copernicus velocity field and rewrites `greenland_ocean_currents_cmems_202508.meta.json` + `.bin` as 498 flowlines / 7,026 segments across six depth horizons.
- 2026-03-08: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so the Greenland toggle now reads `Show 3D ocean flowlines`, the ocean layer renders connected colored line segments instead of arrow glyphs, and `Data Snapshot` now reports flowline counts/segments plus the reference month.
- 2026-03-08: Re-verified the flowline version with `WEB_GAME_CLIENT` and Playwright: Greenland `Balanced -> enable ocean flowlines -> HD -> Balanced` passed without console errors, and screenshots confirm continuous flowlines around Greenland. Artifacts saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-flowlines-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-flowlines-validation/`.
- 2026-03-08: Investigated user report that toggling `Show 3D ocean flowlines` appeared to show nothing. Root cause was visual, not toggle failure: the first flowline export was too sparse, WebGL line width stayed at thin default, and depth-tested lines were often hidden by bed geometry from the default Greenland view.
- 2026-03-08: Increased Greenland ocean-flowline density by updating `scripts/prepare_greenland_ocean_currents.py` defaults to `sample_stride=6`, `seed_target_per_depth=260`, `flowline_max_steps=64`, `min_seed_speed_mps=0.03`, then regenerated `greenland_ocean_currents_cmems_202508.meta.json` + `.bin` to 1,111 flowlines / 22,835 segments.
- 2026-03-08: Made the Greenland flowlines much more visible in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` by cache-busting the ocean-current URLs, increasing opacity, disabling depth testing for the flowline material, and raising render order so flowlines read clearly from the default camera.
- 2026-03-08: Re-verified the visibility fix with `WEB_GAME_CLIENT` and Playwright; the Greenland default camera now shows obvious ocean flowlines immediately after toggling the layer on, with no console errors. Artifacts saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-flowlines-visible-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-flowlines-visible-validation/`.
- 2026-03-08: Replaced the layered 2D ocean-flowline export with true 3D streamline tracing in `scripts/prepare_greenland_ocean_currents.py`. The script now loads sparse 3D `u/v/w/theta/sal` volumes, traces streamlines through `(lon, lat, depth)` using the monthly velocity field, and exports segment endpoints with independent start/end depths plus terminal-arrow flags. Current package is 1,062 3D streamlines / 26,777 segments with depth excursions down to about 1,192 m.
- 2026-03-08: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to render the new 3D streamline package: each ocean segment now uses varying endpoint depths in scene space, colors interpolate smoothly along the line from local temperature/salinity, and streamline termini get brighter arrowheads to indicate flow direction.
- 2026-03-08: Updated Greenland `Data Snapshot` ocean-circulation copy to reflect the new 3D interpretation (`3D traces`, `Seed Depths`, `3D Depth Span`) rather than the older layered-flowline wording.
- 2026-03-08: Verified the 3D streamline version with `WEB_GAME_CLIENT` and Playwright: Greenland default load + toggle-on, a tilted camera view, and HD switching all passed with no console errors. Artifacts saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-3dstreamlines-smoke/`, `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-3dstreamlines-smoke-2/`, `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-3dstreamlines-smoke-3/`, and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-3dstreamlines-validation/`.
- 2026-03-08: Added adaptive deep-basin seeding to `/Users/eddie/Documents/yuwang115.github.io/scripts/prepare_greenland_ocean_currents.py` so Greenland ocean streamlines still use the six fixed seed depths for shelf/intermediate waters but now also add bottom-following seeds in water columns deeper than 1,400 m. The adaptive seed depth follows the local model depth (82% of water-column depth, offset upward from the seabed), keeping bottom traces inside the water column instead of capping them at the former ~1,192 m limit.
- 2026-03-08: Regenerated `static/tools/data/greenland_ocean_currents_cmems_202508.meta.json` + `.bin` after the adaptive-bottom change. Current package is 1,090 3D streamlines / 27,027 segments with a 3D depth span from the surface to ~2,490 m; `coverage.streamlines_by_seed_depth` now reports 28 `adaptive_bottom` traces in addition to the fixed 0/50/150/300/600/1000 m seed layers.
- 2026-03-08: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to cache-bust the regenerated Greenland ocean-current package and to expose the deep-basin sampling in `Data Snapshot` via `Adaptive Bottom Seeds`, so users can distinguish the fixed six seed depths from the added deep-water bottom traces.
- 2026-03-08: Verified the deep-basin ocean-streamline revision with `py_compile`, `WEB_GAME_CLIENT`, and Playwright using the same SwiftShader WebGL flags as the automated client. Greenland `Balanced -> enable 3D ocean flowlines` loads cleanly with no console/page errors, `Data Snapshot` reports the deeper span and adaptive-bottom counts, and screenshots/state are saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-deepstreamlines-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-deepstreamlines-validation/`.
- 2026-03-08: Added a left-panel Greenland ocean-flowline color legend to `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`. Because the ocean flowlines are colored by both temperature and salinity rather than by a single scalar, the legend is implemented as a 2D temperature-salinity swatch instead of a 1D bar: warm/cold on the vertical axis, fresh/salty on the horizontal axis, with colors sampled from the same `oceanCurrentColor()` function used by the rendered flowlines.
- 2026-03-08: Wired the ocean legend to region/dataset changes and metadata prefetch so Greenland shows the current package's temperature/salinity range (currently ~`-1.4 to 8.1 °C`, `30.2 to 35.0 PSU`) even before the ocean-flowline layer is toggled on, while Antarctica hides the section entirely.
- 2026-03-08: Verified the ocean legend with `WEB_GAME_CLIENT` plus Playwright DOM/screenshot checks. Greenland shows the new legend in the left panel and Antarctica hides it; artifacts are under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-legend-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-legend-validation/`.
- 2026-03-08: Updated the control labels in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` from `Show ice flowline` to `Show ice flowlines` and from `Show 3D ocean flowlines` to `Show ocean streamlines` for cleaner terminology. Skipped testing per user request.
- 2026-03-08: Fixed the `CHANNEL DISCHARGE COLOR` legend initialization glitch in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`. Root cause was startup order: `main()` painted the channel legend before loading the `cmocean_matter` table, so the first render used the fallback palette, which visually resembled the ice-velocity legend; once hydrology colors loaded later, the bar jumped to the correct palette.
- 2026-03-08: Resolved the jump by loading hydrology color tables during initial app setup (`Promise.all([loadBedColorTable(), ensureHydrologyColorTables()])`) before the first legend paint. Verified with `WEB_GAME_CLIENT` and Playwright that the channel legend at `Ready` and `Ready + 3s` is identical and distinct from the velocity legend. Artifacts are under `/Users/eddie/Documents/yuwang115.github.io/output/channel-legend-init-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/channel-legend-init-validation/`.
- 2026-03-08: Tidied Greenland-facing copy in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` by rewriting the Greenland intro line and suppressing the redundant `BASIN BOUNDARIES -> Status: Available` row while keeping status text for unavailable/on-demand cases. Skipped testing per user request.
- 2026-03-08: Reworked Greenland ocean streamline seeding in `/Users/eddie/Documents/yuwang115.github.io/scripts/prepare_greenland_ocean_currents.py`. Fixed seed depths now use the requested interpolated depths `0, 100, 250, 500, 750, 1000 m` instead of snapping to the nearest native Copernicus level, so the exported metadata and tracing planes now reflect the requested depths exactly.
- 2026-03-08: Replaced the previous single adaptive-bottom layer with two deterministic deep-water adaptive layers for water columns deeper than `1400 m`: `adaptive_deep_mid` and `adaptive_deep_lower`. Their seed depths vary cell-by-cell using depth fractions plus spatial jitter, so deep-water streamlines are no longer confined to one nearly planar bottom-following layer. Adaptive thresholds were also relaxed (`min_speed` down to `0.004-0.005 m/s`, `min_seed_speed` down to `0.012-0.015 m/s`, `min_trace_speed` down to `0.007-0.008 m/s`) to better retain weak mid- to lower-water currents.
- 2026-03-08: Regenerated `static/tools/data/greenland_ocean_currents_cmems_202508.meta.json` + `.bin` after the new seeding pass. Current package is `927` 3D streamlines / `19,248` segments with fixed seed depths `[0, 100, 250, 500, 750, 1000] m`, `128` adaptive deep traces (`80` mid-depth + `48` lower-depth), and a maximum traced depth of about `2,996 m`.
- 2026-03-08: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to read the new `adaptive_deep_seeding` metadata and summarize it in `Data Snapshot` as `Adaptive Deep Seeds` instead of the older `Adaptive Bottom Seeds`.
- 2026-03-08: Verified the revised ocean streamline seeding with `py_compile`, `WEB_GAME_CLIENT`, and Playwright. Greenland `Balanced -> enable ocean streamlines` loads with no console/page errors; `Data Snapshot` shows `Seed Depths: 0, 100, 250, 500, 750, 1,000 m` plus `Adaptive Deep Seeds: 128`, and screenshots are saved under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-adaptive-seeds-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-adaptive-seeds-validation/`.
- 2026-03-08: Strengthened the Greenland ocean streamline color-space corners in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so warm/cold and fresh/salty regimes are easier to distinguish. The four anchor colors are now more saturated and farther apart in hue, and the global brightness lift was reduced so the corners keep their contrast instead of being washed toward a similar pastel midtone.
- 2026-03-08: Re-verified the updated ocean palette with `WEB_GAME_CLIENT` and Playwright on Greenland. The left-panel ocean color key now shows a more decisive cyan / indigo / green / orange-red separation, the streamline overlay reflects the same palette in-scene, and there were no console/page errors. Artifacts are under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-color-contrast-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-color-contrast-validation/`.
- 2026-03-08: Updated Greenland `Data Snapshot` ocean-copy in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so `Water-Mass Color` now explicitly describes the strengthened four-corner palette (`cold-fresh cyan, cold-salty indigo, warm-fresh green, warm-salty orange-red`) instead of listing only the temperature/salinity range. Verified with `WEB_GAME_CLIENT` and Playwright that the new wording appears correctly in the snapshot, with no console/page errors. Artifacts are under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-data-snapshot-colorcopy-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-data-snapshot-colorcopy-validation/`.
- 2026-03-08: Finished the remaining Greenland ocean-copy cleanup in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`: `Flowline Field` is now `Streamline Field`, `3D flowlines` is now `3D streamlines`, and the ocean legend note now refers to `ocean streamlines` for consistency with the updated UI terminology. Skipped further testing per user request.
- 2026-03-08: Nudged the Greenland ocean legend x-axis labels in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so `Fresh 30.2 PSU` sits slightly to the right of the colormap's lower-left corner while `Salty 35.0 PSU` stays anchored to the lower-right edge. Re-verified with `WEB_GAME_CLIENT` and Playwright; geometry now shows `fresh.left = canvas.left + 6 px` and `salty.right = canvas.right`, with screenshots under `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-label-nudge-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-label-nudge-validation/`.
- 2026-03-08: Corrected the Greenland `Data Snapshot` ocean-circulation text in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so it now uses the current streamline terminology (`3D streamlines`) and pairs with the regenerated adaptive-seed ocean package instead of older cached `1,090 traces / 0,50,150,300,600,1000 m` metadata. Updated the Greenland ocean meta/bin cache-busting query string to `v=20260308-adaptive-seeds-v2`. Skipped testing per user request.
- 2026-03-08: Updated the left legend panel in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so Greenland now labels the bivariate ocean key as `Ocean Streamline Color`, and the hydrology-only legends (`Effective Pressure Color`, `Channel Discharge Color`) are hidden automatically whenever the active region lacks hydrology support. Verified with `WEB_GAME_CLIENT` plus Playwright screenshots that Greenland hides both bars while Antarctica still shows them. Artifacts are under `/Users/eddie/Documents/yuwang115.github.io/output/legend-visibility-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/legend-visibility-validation/`.
- 2026-03-08: Restored normal depth occlusion for Greenland ocean streamlines in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` by re-enabling `depthTest` on the streamline line material. This stops far-side streamlines from rendering through ice and bed while keeping the streamline colors and arrowheads intact. Re-verified with `WEB_GAME_CLIENT` and a Playwright toggle/screenshot run under `/Users/eddie/Documents/yuwang115.github.io/output/ocean-depth-occlusion-smoke/` and `/Users/eddie/Documents/yuwang115.github.io/output/ocean-depth-occlusion-validation/`.
- 2026-03-08: Enabled normal depth occlusion for refined-basin label sprites in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` by switching the basin-label `SpriteMaterial` back to `depthTest: true`. This applies to both Antarctica and Greenland, so basin labels are now hidden correctly behind foreground ice and bed. Skipped testing per user request.
- 2026-03-08: Fixed the follow-on basin-label clipping issue in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`. Refined-basin label sprites now use bottom anchoring (`sprite.center.y = 0`) plus a small world-space lift tied to sprite height, so the text sits fully above the sampled bed/ice surface instead of intersecting it while still obeying normal depth occlusion. Verified with `WEB_GAME_CLIENT` and Playwright screenshots for Antarctica and Greenland under `/Users/eddie/Documents/yuwang115.github.io/output/basin-label-clearance-smoke/`, `/Users/eddie/Documents/yuwang115.github.io/output/basin-label-clearance-validation/`, and `/Users/eddie/Documents/yuwang115.github.io/output/basin-label-clearance-oblique/`.
- NOTE: The 3D streamlines are now traced with real `w`, but Arctic monthly-mean vertical velocities are still much smaller than horizontal velocities. From the default top-down Greenland view, the 3D curvature is physically present but subtler than from an oblique camera angle; the low-angle validation screenshot is `/Users/eddie/Documents/yuwang115.github.io/output/greenland-ocean-3dstreamlines-validation/greenland-balanced-3dstreamlines-low-angle.png`.
- 2026-03-08: Added a dedicated trailer workflow for the explorer. `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` now supports `?mode=trailer`, keeps showcase mode unchanged, accepts `region` + `preset` query params in trailer mode, exposes `window.trailerApi` (`applyState`, `animateCamera`, `waitUntilReady`, `getState`, `setCameraPose`, overlay controls), and includes an in-scene trailer text overlay layer for title cards / lower-third copy.
- 2026-03-08: Added `/Users/eddie/Documents/yuwang115.github.io/scripts/trailer/scenes.json` and `/Users/eddie/Documents/yuwang115.github.io/scripts/trailer/voiceover.md` to lock the v1 8-scene trailer structure, on-screen copy, and English voiceover script.
- 2026-03-08: Added `/Users/eddie/Documents/yuwang115.github.io/scripts/capture_explorer_trailer.mjs`. The script can now drive scenes from the JSON spec, emit state logs + SRT + voiceover text, use Playwright realtime video + ffmpeg for most scenes, and fall back to frame-sequence capture for the interactive UI proof scene.
- 2026-03-08: Verified `trailer mode` and `showcase mode` smoke with `WEB_GAME_CLIENT`. Trailer smoke confirms `mode=trailer`, `region=greenland`, and preset aliasing resolve correctly; showcase smoke confirms the homepage-style Antarctica showcase still reports `mode=showcase` and default locked camera state.
- 2026-03-08: Verified `window.trailerApi` state switching in-browser: Greenland trailer load reaches `ready`, Antarctica `velocity + flowline` toggle-on succeeds via `applyState`, and overlay text appears in returned state.
- 2026-03-08: Installed `ffmpeg` via Homebrew so trailer capture can now transcode scene clips and assemble a master MP4 locally instead of stopping at raw WebM/frame artifacts.
- 2026-03-08: Validated the realtime video path with `scripts/capture_explorer_trailer.mjs --scene antarctica-hero`; after fixing trim logic to derive the cut-in point from the raw video duration, the exported hero clip now lands at ~5 s and 30 fps under `/Users/eddie/Documents/yuwang115.github.io/output/trailer-realtime-smoke-3/`.
- 2026-03-08: Investigated the interactive `ui-proof` scene path. Fixed a null-overlay bug in `updateTrailerOverlay`, removed the screenshot timeout by disabling Playwright's default screenshot limit, and reworked the UI proof sequence to a lighter `Antarctica Balanced -> Antarctica HD -> Greenland Balanced + Data Snapshot` flow. A fresh smoke run is in progress under `/Users/eddie/Documents/yuwang115.github.io/output/trailer-ui-smoke-3/`; the final full-trailer export should use the same lighter scene with `captureFps = 8`.
- 2026-03-08: Finished the v1 trailer pipeline. `window.trailerApi.getState()` now exposes the planned `preset` + `toggles` fields (while preserving the older `dataset` + `controls` aliases), single-scene trailer exports no longer overwrite root-level master assets, the Greenland ocean scene was re-framed to keep 3D ocean streamlines readable in the final cut, and a complete 49 s / 1920x1080 / 30 fps master MP4 plus per-scene clips, state logs, `SRT`, voiceover text, and manifest were written to `/Users/eddie/Documents/yuwang115.github.io/output/trailer-final-v1/`. Final smoke checks for trailer/showcase mode are under `/Users/eddie/Documents/yuwang115.github.io/output/trailer-mode-smoke-final/` and `/Users/eddie/Documents/yuwang115.github.io/output/showcase-regression-smoke-final/`.
- 2026-03-09: Removed the trailer-specific runtime hooks from `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`. The page no longer exposes `mode=trailer`, the in-scene trailer overlay, or `window.trailerApi`; normal interactive mode and homepage showcase mode remain in place, while the trailer capture scripts/assets stay in the repo as offline artifacts only.
- 2026-03-09: Added `scripts/prepare_rise_antarctica.py` to convert the AADC RISE multi-model mean NetCDF into two output families: `static/tools/data/rise_antarctica_preview_index.json` plus pre-rendered Antarctica PNG rasters (`ismr`, `tstar_zice`, `zice`, `mask`) for a lightweight 2D preview, and BedMachine-aligned Antarctica `Balanced/HD` binary packages (`rise_antarctica_480.*`, `rise_antarctica_741.*`) carrying `mask`, `iceshelf_id`, `zice`, `ismr`, and `tstar_zice`.
- 2026-03-09: Added `/Users/eddie/Documents/yuwang115.github.io/static/tools/rise-antarctica-2d.html`, a standalone EPSG:3031 preview page with variable switching, clip modes (`All`, `Ice shelf only`, `Ice shelf + ocean context`), dynamic legends, and source/citation metadata sourced from the generated preview index. Adjusted the viewer to hide the loading veil correctly once rasters are ready and switched the map canvas to a dark ocean backdrop so shelf signals remain legible.
- 2026-03-09: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to add Antarctica-only RISE overlays: new `Show basal melt` and `Show thermal driving` toggles, Antarctica preset URLs for the new RISE packages, metadata/source reporting in `Data Snapshot`, Antarctica-only legend sections, and on-demand loading/build of melt/thermal-driving meshes attached to the ice-bottom surface. Greenland now disables and hides the RISE-only controls/legends. Validation artifacts are under `/Users/eddie/Documents/yuwang115.github.io/output/rise-validation/`.
- 2026-03-09: Refined the RISE basal-melt visualization to use a zero-centered asymmetric diverging scale instead of the earlier symmetric `-3.9 .. +3.9 m/yr` stretch. The new preview and Antarctica overlay metadata now target about the 90th percentile on each sign separately, yielding roughly `-0.52 .. +1.82 m/yr` on the native 2 km preview and about `-0.56 .. +1.80 m/yr` on the Antarctica Balanced overlay, with `0 m/yr` fixed at the center of the colorbar.
- 2026-03-09: Changed only the 3D Explorer basal-melt rendering in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to a fixed signed-log (`symlog`-style) visualization clipped to `-1 .. 15 m/yr`, with `0` held at the exact midpoint and the legend switched to non-linear tick spacing plus an explicit scale note. The 2D RISE preview and generated metadata packages were intentionally left unchanged. During validation this also exposed and fixed a UI formatting bug where `formatCoord(value, 0)` could incorrectly collapse `10` to `1`, so the new legend now renders clean tick text (`-1, -0.3, -0.1, 0, 0.3, 1, 3, 15`).
- 2026-03-09: Added surface-toggle coupling in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so enabling `Show basal melt` or `Show thermal driving` now auto-clears `Show ice surface`, while enabling `Show effective pressure` or `Show subglacial channels` auto-clears both `Show ice surface` and `Show ice bottom surface`. This was implemented only in the user-triggered checkbox `change` handlers plus a shared ice-surface visibility sync helper; testing was skipped per user request.
- 2026-03-09: Updated the left-panel RISE wording in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`: the metadata section header now reads `Ice-Shelf Basal Melt`, and the `Sources` line now reads `Melt Drivers: Galton-Fenzi et al. (2025) Multi-model estimate of Antarctic ice-shelf basal melting and ocean drivers, V1 (15,269 ice-shelf cells)`. Testing was skipped per user request.
- 2026-03-09: Removed the `Active Overlay` row from the left-panel `Ice-Shelf Basal Melt` metadata section in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`. Testing was skipped per user request.
- 2026-03-09: Removed the left-panel `Basin Boundaries` metadata section, removed the matching `Basin Boundaries` line from `Sources`, and deleted the `Pressure Coverage` row from `Subglacial Hydrology` in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`. Browser testing was not run after this text-only cleanup.
- 2026-03-09: Made the left-panel `Data Snapshot > Sources` entries clickable in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html`. Region `sources` config now supports `{ text, url }`, rendered links open official dataset pages in a new tab, and dynamic suffixes like ` (15,269 ice-shelf cells)` and ` (n 3D streamlines)` remain outside the anchor. Added minimal in-panel link hover/focus styling and verified in-browser that Antarctica (4 links) and Greenland (3 links) render correctly, open new tabs with the expected official URLs, and produce no console/page errors. Validation artifacts are in `/Users/eddie/Documents/yuwang115.github.io/output/source-links-validation/`.
- 2026-03-09: Restored the Antarctica-only `Sources > Basin Boundaries` row in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` after it had been removed too broadly. The row now links to the NSIDC `MEaSUREs Antarctic Boundaries for IPY 2007-2009, Version 2` landing page and is rendered only for Antarctica so Greenland's `Sources` block stays unchanged. Added a focused browser re-check for the restored link.
- 2026-03-09: Updated `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` so legend panels now follow the visible overlays instead of being statically shown by region capability. The initial view only shows the bed-elevation legend, the velocity legend appears when either `Show ice surface velocity` or `Show ice flowlines` is enabled, and the RISE/ocean/hydrology legends now appear only while their corresponding toggles are active. This required wiring dedicated DOM refs for the bed/velocity legend sections, adding a shared `updateLegendVisibility()` helper, and invoking it during region/dataset changes, metadata refreshes, and relevant checkbox handlers. Validation pending.
- 2026-03-09: User clarified that when multiple compatible overlays are enabled at the same time, all of their corresponding colorbars should remain visible together. Confirmed the current implementation already behaves that way because each legend section is controlled independently in `updateLegendVisibility()`; no additional code changes were required. Testing was skipped per user request.
- 2026-03-09: Renamed the first `Data Snapshot` group header in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` from `Grid & Topography` to `Geometry & Grid` to better match the included preset/grid/projection/bed/ice-thickness fields. This was a text-only change; browser testing was skipped per user request.
- 2026-03-09: Restored the refined basin label rendering in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to the pre-`7840012` behavior: basin labels no longer use depth testing, the temporary bottom-anchor plus `labelLiftUnits` lift was removed, and label y-positions now return to the original exaggeration-only placement. A focused `WEB_GAME_CLIENT` run with `Show refined basins` enabled confirmed the basin overlay still loads (`meshes.basins: true`) and the screenshot shows labels visible above the ice surface without the previous artificial lift. Validation artifacts are in `/Users/eddie/Documents/yuwang115.github.io/output/basin-label-restore-validation/`.
- 2026-03-09: Reworked the `Ice Surface Velocity Color` visualization in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` to use a fixed `0..3000 m/yr` nonlinear scale for both the mesh colors and the legend. The old `VELOCITY_REFERENCE_SPEED = 1800` mapping was replaced with a shared positive log-like configuration (`knee = 20 m/yr`, clipped at `3000 m/yr`), and the legend now renders explicit nonlinear ticks at `0, 30, 100, 300, 1000, 3000` plus a scale note. Validation included a `WEB_GAME_CLIENT` Antarctica Balanced run with `Show ice surface velocity` enabled and a focused panel capture confirming `legends.velocity: true`, no console/page errors, and the new tick layout. Artifacts are in `/Users/eddie/Documents/yuwang115.github.io/output/velocity-scale-validation/` and `/Users/eddie/Documents/yuwang115.github.io/output/velocity-scale-panel/`.
- 2026-03-09: Changed the velocity surface overlay in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` from vertex-colored `speed` interpolation to continuous fragment-shader sampling of the underlying `vx/vy` field. The geometry worker now returns per-vertex UVs, the main thread uploads `vx`, `vy`, and validity into a float/half-float `DataTexture`, and the shader computes `speed = length(interp(vx, vy))` with the same nonlinear `0..3000 m/yr` visualization used by flowlines. Unsupported texture-sampling environments fall back to the older vertex-colored mesh, and velocity textures are now disposed on layer rebuilds / dataset switches / load failures. Validation covered Antarctica Balanced with both `Show ice surface velocity` and `Show ice flowlines`, Antarctica HD, and Greenland HD (`1km`) with no console/page errors; artifacts are in `/Users/eddie/Documents/yuwang115.github.io/output/velocity-continuous-smoke-balanced/` and `/Users/eddie/Documents/yuwang115.github.io/output/velocity-continuous-validation/`.
- 2026-03-09: Reduced the `Show ice surface velocity` UI freeze risk in `/Users/eddie/Documents/yuwang115.github.io/static/tools/antarctica-bedmachine-3d.html` by making velocity toggle loads non-blocking: the toggle no longer opens the blocking viewer loading overlay, and the continuous `vx/vy` shader path is now capped to grids up to `1,000,000` cells so large HD products fall back to the lighter vertex-colored mesh instead of uploading very large velocity textures on the main thread. The shader path also now prefers half-float textures before full float to cut upload cost. Simplified validation rechecked the default Antarctica Balanced toggle path and confirmed that immediately after clicking `Show ice surface velocity` the loading overlay remains hidden, the UI stays responsive, `meshes.velocity: true` is reached, and no console/page errors were emitted. Artifacts are in `/Users/eddie/Documents/yuwang115.github.io/output/velocity-freeze-fix-validation/`.
- TODO: Add Greenland hydrology layers and, if needed later, region-appropriate basin overlays.
- TODO: Consider a Greenland HD preset if higher local detail is needed.
- TODO: If monthly ocean-current browsing is needed later, add a time selector and regenerate/swap the Copernicus ocean-current package by month rather than hard-wiring the current August 2025 snapshot.
